apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  labels:
    app: workshop
    component: frontend
spec:
  type: NodePort
  selector:
    app: workshop
    component: web
  ports:
    - name: http
      port: 80
      targetPort: http
      nodePort: 30080
      protocol: TCP